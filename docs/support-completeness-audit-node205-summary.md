# Summary

`ManagedStageSupportCompletenessAudit.v1` is emitted from the topology-gated support export result. It provides per-support proof rows and node 205 counters so missing supports can be diagnosed from the generated audit JSON instead of by visual inspection only.
